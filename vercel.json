{
  "buildCommand": "pnpm --filter @repo/db generate && cd apps/evrydayarchive-web && next build",
  "outputDirectory": "apps/evrydayarchive-web/.next",
  "installCommand": "pnpm install --frozen-lockfile"
}
